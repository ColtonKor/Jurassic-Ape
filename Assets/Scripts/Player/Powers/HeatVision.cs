using UnityEngine;

public class LaserBeams : Superpowers
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        currentCapacity = maxCapacity;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
